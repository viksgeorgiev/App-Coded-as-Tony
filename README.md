# Personal Task Manager

Simple local Next.js + SQLite todo app (create, edit, delete). Run locally.

Commands:

```bash
npm install
npx prisma migrate dev --name init
npm run dev
```

Open http://localhost:3000
